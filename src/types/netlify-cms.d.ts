// Type definitions for Netlify CMS
interface Window {
  CMS_MANUAL_INIT?: boolean;
  CMS: any; // You can replace 'any' with more specific types if needed
}

declare const CMS: any; // You can replace 'any' with more specific types if needed
